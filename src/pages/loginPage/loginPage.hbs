{{#> mainLayout}}
{{#> formLayout title="Вход"}}
  {{#> form className="login-form"}}
    <div class="form__fields">
      {{#> formField label="Логин" errorMessage="Неверный логин"}}
        {{> input type="text" name="login" placeholder="Введите логин"}}
      {{/formField}}
      {{#> formField label="Пароль" errorMessage="Неверный пароль"}}
        {{> input type="password" name="password" placeholder="Пароль"}}
      {{/formField}}
    </div>
    <div class="form__actions">
      {{> button className="primary login-form__button" type="submit" text="Войти"}}
      {{#> link href="/signup" className="form__link"}}
        Нет аккаунта?
      {{/link}}
    </div>
  {{/form}}
{{/formLayout}}
{{/mainLayout}}
